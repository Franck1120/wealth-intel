'use client';

/**
 * Report viewer component - placeholder for future enhanced report display.
 * Currently, the reports page handles rendering directly as a server component
 * with all report data expanded inline.
 *
 * Future enhancements:
 * - Collapsible/expandable sections
 * - PDF export
 * - Share via email
 */
export function ReportViewer() {
  return null;
}
